{
  "name": "GrupoProduto",
  "type": "object",
  "properties": {
    "group_id": {
      "type": "string",
      "description": "ID do grupo empresarial (global)"
    },
    "empresa_id": {
      "type": "string",
      "description": "ID da empresa que cadastrou (cadastro único compartilhado no grupo)"
    },
    "nome_grupo": {
      "type": "string",
      "description": "Nome do grupo — único por grupo (Ex: Ferragens, Bitolas, Materiais Elétricos)"
    },
    "codigo": {
      "type": "string",
      "description": "Código sequencial único do grupo"
    },
    "natureza": {
      "type": "string",
      "enum": [
        "Revenda",
        "Produção",
        "Consumo",
        "Serviço"
      ],
      "default": "Revenda",
      "description": "Natureza dos produtos deste grupo"
    },
    "ncm_padrao": {
      "type": "string",
      "description": "NCM padrão para produtos deste grupo (V16.1)"
    },
    "conta_contabil_padrao_id": {
      "type": "string",
      "description": "Conta contábil padrão (V16.1)"
    },
    "margem_sugerida": {
      "type": "number",
      "description": "% de margem sugerida para produtos deste grupo"
    },
    "icone": {
      "type": "string",
      "description": "Nome do ícone (lucide-react)"
    },
    "cor": {
      "type": "string",
      "description": "Cor hexadecimal para UI"
    },
    "ativo": {
      "type": "boolean",
      "default": true,
      "description": "Se o grupo está ativo"
    },
    "observacoes": {
      "type": "string"
    }
  },
  "required": [
    "nome_grupo",
    "natureza"
  ],
  "rls": {
    "read": {
      "$or": [
        {
          "data.group_id": "{{user.data.group_id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "data.group_id": "{{user.data.group_id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "data.group_id": "{{user.data.group_id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}